{
  "name": "forestplant-desktop",
  "version": "0.2.0",
  "private": true,
  "description": "Export of forest fund data from the GIS forestplant.gharysh.kz into KML",
  "type": "module",
  "main": "src/main/bootstrap.cjs",
  "scripts": {
    "start": "node scripts/start.js",
    "load": "node src/cli/load.js --raw db/raw --db db/forest.sqlite",
    "test": "node --test test/*.test.js",
    "smoke": "node scripts/start.js --smoke",
    "sync": "node src/cli/sync.js --raw db/raw --db db/forest.sqlite",
    "dist": "electron-builder --mac --win --publish never",
    "dist:mac": "electron-builder --mac --publish never",
    "dist:win": "electron-builder --win --publish never",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "electron": "^43.4.1",
    "electron-builder": "^26.15.3",
    "eslint": "^10.9.0",
    "globals": "^17.11.0"
  },
  "author": "KazLP",
  "license": "UNLICENSED"
}

# Dependencies
node_modules/

# Data: the database and the raw archive. Gigabytes, restored by syncing —
# no place for them in the repository.
db/
raw/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Builds and exports
dist/
out/
*.dmg
*.exe
*.AppImage
kml/
*.kml
*.geojson

# Credentials and local settings — must never enter the history
.env
.env.*
settings.json
credentials.json

# System
.DS_Store
Thumbs.db
*.log
*.part
